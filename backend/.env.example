PORT=5000
MONGODB_URI=mongodb+srv://neuroverse_db:<db_password>@neuroverse.coatw81.mongodb.net/neuroverse?retryWrites=true&w=majority&appName=Neuroverse
NODE_ENV=development

# Firebase Admin (server-side)
# Option A (recommended): path to the service account JSON (keep this file OUT of git)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json

# Option B: raw JSON string (useful on Render/Railway). Do NOT commit.
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Legacy JWT settings (no longer required if using Firebase Auth)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=30d

# Email Configuration (for notifications)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
